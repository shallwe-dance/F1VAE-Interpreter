# F1VAE
Supported:
- first-class functions
- variables
- arithmetics
- expressions
```
def func1 = 3 endef #defines a function with no input, always returns 3
def func2 x y = x+y endef #defines a function with two input, which returns sum of the inputs

let x = f1() in f2(1,x)
#Result : x=4
```


